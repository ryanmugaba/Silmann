-- Silman production seed: intentionally empty.
-- Do NOT load demo users or sample NDIS data in production.
--
-- New customers: sign up at /signup → onboarding → Stripe checkout at /settings/billing
--
-- Optional local fixtures (dev only):
--   NEXT_PUBLIC_ENABLE_MOCK_DATA=true
--   Never run historical demo seed against a live Supabase project.

SELECT 1;
