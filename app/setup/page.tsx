import { SetupConnectionClient } from "@/components/setup/setup-connection-client";

export const metadata = {
  title: "Connect Silman — Setup",
};

export default function SetupPage() {
  return <SetupConnectionClient />;
}
