# =============================================================================
# Silman — copy this file to `.env.local` and fill in real values.
# Production: set the same variables in Vercel → Project → Settings → Environment Variables
# =============================================================================

# ─── Required: Supabase (region: Australia Sydney / ap-southeast-2) ───────────
# https://supabase.com/dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Required: App URL (must match your deployed domain for Stripe redirects) ─
# Local:  http://localhost:3000
# Vercel: https://your-app.vercel.app  or  https://app.yourdomain.com.au
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Required: Stripe (subscriptions — 14-day trial, then $29.99 AUD/month) ───
# Dashboard: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=REPLACE_WITH_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=REPLACE_WITH_STRIPE_WEBHOOK_SECRET
# Products → Silman Pro → recurring price $29.99 AUD/month → copy Price ID
STRIPE_PRICE_ID=price_xxxxxxxxxxxxxxxx
# Optional: override trial length (default 14 days, set in code via Checkout)
# STRIPE_TRIAL_DAYS=14
# Optional: publishable key (Checkout uses redirect; keep for future Stripe.js)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=REPLACE_WITH_STRIPE_PUBLISHABLE_KEY

# ─── Required for AI features ─────────────────────────────────────────────────
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini

# ─── Required on Vercel for scheduled reminders cron ──────────────────────────
# Generate: openssl rand -hex 32
CRON_SECRET=replace-with-a-long-random-string

# ─── Optional: email notifications ───────────────────────────────────────────
RESEND_API_KEY=
RESEND_FROM_EMAIL=Silman <notifications@yourdomain.com.au>

# ─── Optional: SMS (later) ───────────────────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ─── Optional: local UI fixtures only (never enable in production) ─────────────
# NEXT_PUBLIC_ENABLE_MOCK_DATA=false
